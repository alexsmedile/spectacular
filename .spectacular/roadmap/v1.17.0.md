## v1.17.0 — Roadmap ledger + Decisions index

**Tier:** themed
**Status:** shipped (2026-06-16)
**Phase:** release

**Outcome:**
Two housekeeping items that sharpen the operational substrate. Roadmap ledger makes `build → version` the single source of truth (one edit to reslot a request, not ~14 refs). Decisions index mode splits a flat `DECISIONS.md` into a cheap index + per-entry files when it grows large.

**Shipped (2026-06-16):**
- `roadmap-ledger` (b7) — ledger table as single source of truth; `spectacular roadmap` reads from ledger; `spectacular new` stamps build ids; `doctor links` flags stray version refs
- `decisions-index` (b9) — index mode (`decisions/D<N>.md` + cheap root index); `spectacular decisions migrate`; `doctor decisions` area

> **Note:** `cli-debt-removal` (b4) was originally drafted into this slot but shipped separately as **v1.23.2** (verified + archived 2026-06-30) — see the ledger row and CHANGELOG.

**Linked requests:**
- roadmap-ledger (b7, shipped)
- decisions-index (b9, shipped)

---

> **Reconciliation note (2026-06-27):** v1.18.0 shipped the **SPEC.md drift check** (see [`CHANGELOG.md`](../CHANGELOG.md)), not "Contract prep ①" as an earlier draft labelled it. The contract-prep ladder is `target: tbd` (ordered ①→②→③, not version-pinned); the coherence batch (b15/b13/b14/b12) shipped ahead of it as v1.19–v1.22.

---
