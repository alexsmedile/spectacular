# Orient

Compile project context with `@Orient`. Lead with the project Anchor's plain-language direction,
boundaries, and constraints. Show current Mission pointers, visible Gaps/conflicts, freshness, and
the single continuation or owner gate. Do not preload every record or narrate historical rationale.

If one Mission is selected, compile that exact Mission scope and follow its pointers. If several
Missions exist or no continuation is mechanically justified, stop at the returned selection or
authority gate. Drill down with noun-first `show` operations; use `workspace validate <scope>` only
for scoped integrity.
