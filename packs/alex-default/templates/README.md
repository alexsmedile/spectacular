# <project-name>

One-line description.

**Type**: cli | script | automation | app | lib | skill | plugin | content | research | vault-project
**Stack**: e.g. Python 3.11 | Node 20 | Bash
**Run**: one-liner to execute (e.g. `python main.py --help`)

## What it does

2-3 sentences. Input → transformation → output. Be concrete about the user-visible effect.

## Setup

Steps to install dependencies and configure env vars.

```bash
# example
```

## Usage

Minimal working example — the first thing a new user runs to confirm it works.

```bash
# example invocation
```

## Status

Type: see header. License: see `LICENSE`. Issues: <repo>/issues.
