---
updated: 2026-08-06
---

# Verify log — soft-db-projection-inventory

## 2026-08-06 — walk (5 passed, 0 blocked, 0 skipped)

- ✅ [assert] #11 remains the sole workspace-orientation route — PLAN, inventory, SKILL.md, and soft-DB guidance consistently name `spectacular status --brief --json`; no competing briefing command or generic query route was added.
- ✅ [judge] Entity details match the approved projection contract — the inventory's named decision and literal `--full` escalation are implemented for decisions, memories, questions, research, spikes, ideas, audits, and fixes; selection remains frontmatter-led.
- ✅ [exec] Projection omission regression — `bash tests/cli/read-projections.test.sh` exit 0; 23 assertions passed.
- ✅ [exec] Bash 3.2 syntax and guarded version consistency — `bash -n cli/spectacular cli/install.sh scripts/hooks/pre-commit` and `scripts/hooks/pre-commit --check` exit 0.
- ✅ [assert] Scope remains bounded — no filters, derived index, cache, broad YAML parser, or additional workspace briefing is present; the re-run baseline retains the #11 orientation route.

**Outcome:** verified — all validation checks passed against `d76c815`.
