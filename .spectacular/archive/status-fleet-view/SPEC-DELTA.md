### ADDED
- **`spectacular status` deterministic fleet view (v1.29.0+)** — bare `status` renders the active-request fleet directly from each `requests/<slug>/PLAN.md` frontmatter plus grep-safe body signals (the `## Goal` line, `x/total` top-level task progress with `- [~]` counted separately as deferred, and the current milestone = first `### M<N>` with an open top-level task), sorted active→planned. `status <slug>` prints a single request card (goal · summary · progress · current milestone · `related:` deps · stale flag when active >14d); `status --json` emits the structured fleet — the agent opt-in contract for discovering requests without opening files. Replaces the hand-cached CLAUDE.md Active Requests table (generator-as-source-of-truth). `--against-latest` / `--since` unchanged. See [[status]].
- **Enforced PLAN/TASKS structure (v1.29.0+)** — PLAN uses canonical **unnumbered** section headings in order (`## Goal / ## Constraints / ## Milestones / ## Tasks / ## Dependencies / ## Validation / ## Deliverables`; extra sections allowed between). TASKS groups by `### M<N>` with flush-left `- [ ]`/`- [x]`/`- [~]` checkboxes (`[~]` = deferred, documented). `doctor` (lifecycle area) **errors** on structure drift for active requests (missing/mis-ordered sections, missing `### M`, malformed checkboxes); `.spectacular/archive/` is skipped. `doctor --fix` de-numbers legacy `## 1. Goal` headings. The status body-grep assumes this schema, so no graceful-degradation branches are needed. See [[plan-rules]], [[tasks-rules]].

### MODIFIED
- SPEC.md :: "**CLI mutator verbs (v0.7.0+)** — verbs replace skill-side manual file edits for lifecycle mutations: `spectacular new <slug>` (scaffolds request; `--target-version` flag in v0.7.1+), `spectacular advance <slug> [--to <state>]` (advances lifecycle; renamed from `promote` in v1.19.0, which remains a deprecated alias), `spectacular undo [--dry-run]` (reverses the last advance/archive/idea-promote via a gitignored `.last-mutation` breadcrumb — single-level, refuses on a stale breadcrumb; v1.22.0+), `spectacular next` (read-only — names the single highest-priority next action; v1.19.0+), `spectacular snapshot <file>` (versioned copy + bump), `spectacular archive <slug>` (move + inbound link rewrite), `spectacular touch <file>` (updated: bump). Backed by shared frontmatter helpers (`fm_get`, `fm_set`, `fm_unset`, `fm_touch`). **Mutation principle:** skill orchestrates; CLI mutates. Reference docs (new-request, archive, lifecycle, versioning) updated to instruct verb usage. Only `status` and `remember` remain as skill stubs (AI judgment required)." -> "**CLI mutator verbs (v0.7.0+)** — verbs replace skill-side manual file edits for lifecycle mutations: `spectacular new <slug>` (scaffolds request; `--target-version` flag in v0.7.1+), `spectacular advance <slug> [--to <state>]` (advances lifecycle; renamed from `promote` in v1.19.0, which remains a deprecated alias), `spectacular undo [--dry-run]` (reverses the last advance/archive/idea-promote via a gitignored `.last-mutation` breadcrumb — single-level, refuses on a stale breadcrumb; v1.22.0+), `spectacular next` (read-only — names the single highest-priority next action; v1.19.0+), `spectacular snapshot <file>` (versioned copy + bump), `spectacular archive <slug>` (move + inbound link rewrite), `spectacular touch <file>` (updated: bump). Backed by shared frontmatter helpers (`fm_get`, `fm_set`, `fm_unset`, `fm_touch`). **Mutation principle:** skill orchestrates; CLI mutates. Reference docs (new-request, archive, lifecycle, versioning) updated to instruct verb usage. `remember` remains a pure skill stub (AI judgment required); `status` gained CLI-mechanical surfaces (bare fleet table / `<slug>` card / `--json`) in v1.29.0 while the richer conversational briefing stays skill-side."

### NOTES
- No REMOVED bullets — this request is additive (new command surfaces + a new enforcement) and tightens an existing convention rather than removing anything.
