### MODIFIED
- SPEC.md :: "Skill proactively detects signals (all TASKS items checked → propose `review`; stale `active` requests → flag)." -> "Skill proactively detects signals (all TASKS items checked → propose `review`; stale `active` requests → flag). Archiving runs a **closure gate** (v1.28.0+): `spectacular archive` blocks on open TASKS, an unwalked VERIFY, or a missing `SPEC-DELTA.md`, each overridable once with `--override <check> --reason` (recorded in `archive_overrides:`)."
- SPEC.md :: "The `specs` area also flags SPEC.md date-drift vs the newest archive (v1.18.0+)" -> "The `specs` area validates `SPEC-DELTA.md` integrity (MODIFIED/REMOVED quote an existing bullet; ADDED isn't a duplicate) as the primary drift signal (v1.28.0+), with SPEC.md date-drift vs the newest archive (v1.18.0+) kept as a backstop"
