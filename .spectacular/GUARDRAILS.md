# Spectacular project guardrails

@Orient @Prepare @Start @Resume @Run @Assess @Reconcile @Resolve

Preserve UUID identity, exact revision fingerprints, source drill-down, owner
authority, provider boundaries, and recoverable writes. Prefer the smallest
human-readable workspace structure that keeps those invariants visible.

Do not add v1 compatibility, generic record/search commands, an authoritative
projection, or a second product root.
